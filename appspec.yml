version: 0.0
os: linux
files:
  - source: /target/awsproject-1.0-SNAPSHOT.war
    destination: /tmp/codedeploy-deployment-staging-area/ /usr/share/tomcat/webapps/
hooks:
  BeforeInstall:
    - location: scripts/install_dependencies
      timeout: 300
  ApplicationStart:
    - location: scripts/write_codedeploy_config.sh
    - location: scripts/start_server
      timeout: 300